#define DEF_WORKTIPS_VERSION_TAG "@VERSIONTAG@"
#define DEF_WORKTIPS_VERSION "2.0.0"
#define DEF_WORKTIPS_RELEASE_NAME "Watchtower"
#define DEF_WORKTIPS_VERSION_FULL DEF_WORKTIPS_VERSION "-" DEF_WORKTIPS_VERSION_TAG

#include "version.h"

const char* const WORKTIPS_VERSION_TAG = DEF_WORKTIPS_VERSION_TAG;
const char* const WORKTIPS_VERSION = DEF_WORKTIPS_VERSION;
const char* const WORKTIPS_RELEASE_NAME = DEF_WORKTIPS_RELEASE_NAME;
const char* const WORKTIPS_VERSION_FULL = DEF_WORKTIPS_VERSION_FULL;
